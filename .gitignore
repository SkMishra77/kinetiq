# venv
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/

# secrets / runtime data
.env
.env.*
!.env.example
# Only the top-level runtime `data/` dir (holds kinetiq.db and backups).
# The `src/kinetiq/data/` seed YAMLs are tracked — do NOT ignore them.
/data/
*.db
*.db-wal
*.db-shm

# IDE
.idea/
*.iml
.vscode/

# build
dist/
build/
*.egg-info/
